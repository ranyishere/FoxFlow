rules particle_rules {

move_node := (start : StartType) << x, y >>  
    -> (start : StartType) << dx, dy >> solving (dx := D(x,t), dy := D(y,t)) {

        dx : ODE = 0.1
        dy : ODE = 0.1

    }

}
